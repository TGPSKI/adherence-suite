"""Vendored curses-TUI primitives (stdlib only).

Source: leather/examples/14-sig-triage/eval/scripts/tui (framework.py,
charts.py, fmt.py, windows.py), copied verbatim so this suite runs from a
fresh clone with no external path dependency. Upstream owns the API; keep
edits there and re-copy rather than diverging here.
"""
