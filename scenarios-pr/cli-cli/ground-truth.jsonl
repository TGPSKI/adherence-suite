{"pr": 13988, "title": "Fix `RESTWithNext` error type, repairing `gh status` and attestation retries", "base_commit": "55dbb4dc6b7edb10b48e3d7fc5bccd32318d1b55", "merge_commit": "e83adbc0642994fae7c39a9a012eb34b8c81f4f1", "prompt": "Fix `RESTWithNext` error type, repairing `gh status` and attestation retries\n\n## Description\r\n\r\nThe `api.Client.RESTWithNext` method did not handle errors in the same way as `api.Client.REST`. The latter wrapped the `go-gh` error into a new `api` error and the former simply returned it. From an error message point of view, there was no visible difference but programmatic checks for `errors.As(err, &api.HTTPError{})` would be true for `REST` and false for `RESTWithNext`.\r\n\r\nI cannot find any reason for this to have been intentional. I suspect what happened was that `RESTWithNext` previously used to handle this case via the following removed code:\r\n\r\n```go\r\n\tsuccess := resp.StatusCode >= 200 && resp.StatusCode < 300\r\n\tif !success {\r\n\t\treturn \"\", HandleHTTPError(resp)\r\n\t}\r\n```\r\n\r\nBut at some point `go-gh` was created, and the `go-gh` api client `Request`/`RequestWithContext` methods began returning errors for non success http status codes.\r\n\r\nThis broke two behaviours:\r\n * `gh status` is supposed to gracefully degrade if notifications or events are inaccessible.\r\n * `gh attestation` commands is supposed to retry attestation fetching for 500 status codes.\r\n\r\n## Notes\r\n\r\nThis is a prerequisite for consolidating GitHub API URL derivation onto `api.Client` (part of the groundwork for per-host API routing, #13717 / #13760), but it is a user-facing bugfix in its own right and is worth reviewing as such.", "test_files": ["api/client_test.go", "pkg/cmd/attestation/api/client_test.go", "pkg/cmd/status/status_test.go"], "test_cmd": "go test ./api/... ./pkg/cmd/attestation/api/... ./pkg/cmd/status/...", "code_files": ["api/client.go"], "additions": 156, "verified": "fails at parent, passes with the fix", "grader": "unit", "invented_symbols": []}
{"pr": 13987, "title": "Replace Windsurf with Devin in skill agents", "base_commit": "b1a9f7b183c12ae111ea63048d49aad2a53e9101", "merge_commit": "89f3699581659f514c4276bef0820d8caadbb67d", "prompt": "Replace Windsurf with Devin in skill agents\n\nFixes #13986\n\n- Replace the `windsurf` agent entry with `devin`\n- Install Devin skills in `.devin/skills` at project and user scope\n- Update install help text and registry tests\n\n## Testing\n\n- `go test ./internal/skills/... ./pkg/cmd/skills/...`", "test_files": ["internal/skills/registry/registry_test.go"], "test_cmd": "go test ./internal/skills/registry/...", "code_files": ["internal/skills/registry/registry.go", "pkg/cmd/skills/install/install.go"], "additions": 25, "verified": "fails at parent, passes with the fix", "grader": "unit", "invented_symbols": []}
{"pr": 13967, "title": "Fix skill picker label wrapping", "base_commit": "01b79dd983af0859e4e3d7454961ad3f08cf88b4", "merge_commit": "c2ad3b0eb7ead66eec3a8a61239e10a765332ec7", "prompt": "Fix skill picker label wrapping\n\nTruncates skill picker labels to the available terminal width so descriptions do not wrap onto a separate line.\r\n\r\nAdds coverage for multiple terminal widths.\r\n\r\n**Before**\r\n<img width=\"597\" height=\"560\" alt=\"Screenshot 2026-07-24 at 16 07 51\" src=\"https://github.com/user-attachments/assets/2cf4f67e-2e40-435b-8c98-cbce5ba4d9c8\" />\r\n\r\n**After (Examples with varying terminal width)**\r\n<img width=\"577\" height=\"560\" alt=\"Screenshot 2026-07-24 at 16 06 22\" src=\"https://github.com/user-attachments/assets/30f29c05-fd19-4672-bc38-c4dfcfe7c60a\" />\r\n<img width=\"856\" height=\"560\" alt=\"Screenshot 2026-07-24 at 16 06 43\" src=\"https://github.com/user-attachments/assets/636db715-823f-487b-80df-4f959ba7a5e3\" />\r\n<img width=\"1285\" height=\"560\" alt=\"Screenshot 2026-07-24 at 16 07 21\" src=\"https://github.com/user-attachments/assets/1e88f7d8-72d8-4037-bed4-ad16b7bfaf96\" />", "test_files": ["pkg/cmd/skills/install/install_test.go"], "test_cmd": "go test ./pkg/cmd/skills/install/...", "code_files": ["pkg/cmd/skills/install/install.go"], "additions": 50, "verified": "fails at parent, passes with the fix", "grader": "cli", "invented_symbols": ["multiSelectLabelMargin"]}
{"pr": 13864, "title": "Add Grok skill host support", "base_commit": "2b970995a3c63b7a7600f242d55d15185a82ed8b", "merge_commit": "9d95dd7344abe6388b59e25b8db54a3c7371cd6a", "prompt": "Add Grok skill host support\n\n## Summary\n- add Grok as a supported `gh skill` agent host\n- install project and user skills in `.grok/skills`\n\nFixes #13846", "test_files": ["internal/skills/registry/registry_test.go"], "test_cmd": "go test ./internal/skills/registry/...", "code_files": ["internal/skills/registry/registry.go", "pkg/cmd/skills/install/install.go", "pkg/cmd/skills/update/update.go"], "additions": 26, "verified": "fails at parent, passes with the fix", "grader": "unit", "invented_symbols": []}
{"pr": 13823, "title": "Add named field columns to gh project item-list", "base_commit": "bba0816a3808c7f7ea0a7e75be9d365b3410c524", "merge_commit": "efe3f165dd297c85fff11473dbf586f2d39fbf86", "prompt": "Add named field columns to gh project item-list\n\nProposal: cli/cli#13816\nTracking issue: github/planning-tracking#3431\n\n## What this does\n\nAdds named field columns to `gh project item-list`:\n\n```\ngh project item-list <project-number> --owner <login> --field \"Status\"\n```\n\n- `--field <name>` (repeatable) resolves the field by name and adds a column showing each item's value for it, with no separate field-ID preflight lookup.\n- `--field-id <id>` does the same by node ID. The two are mutually exclusive, and an unknown `--field-id` lists candidate fields as `name (id)` for symmetry with the name-based errors.\n- Name matching is case-insensitive, mirroring the Projects v2 platform API.\n\n## Relationship to cli/cli#13807\n\nThis builds on the same shared name-resolution layer as the `item-edit` PR cli/cli#13807. To keep the two PRs independently mergeable, each one adds its own copy of the small, self-contained field-resolver file `<path>` (~90 lines: `ResolveFieldByName`, `ResolveFieldByID`, and their error types), with identical contents in both. Because the copies match exactly, Git merges them into a single file automatically, so the two PRs can land in either order without conflicts or duplicate-definition build errors. The item-list-only `FieldValueNodes.DisplayValue` helper lives in its own `<path>`, so this PR leaves `<path>` untouched.", "test_files": ["pkg/cmd/project/item-list/item_list_test.go", "pkg/cmd/project/shared/queries/queries_test.go", "pkg/cmd/project/shared/queries/resolve_fields_test.go"], "test_cmd": "go test ./pkg/cmd/project/item-list/... ./pkg/cmd/project/shared/queries/...", "code_files": ["pkg/cmd/project/item-list/item_list.go", "pkg/cmd/project/shared/queries/queries.go", "pkg/cmd/project/shared/queries/resolve_fields.go"], "additions": 813, "verified": "fails at parent, passes with the fix", "grader": "cli", "invented_symbols": ["fields", "DisplayValue"]}
{"pr": 13807, "title": "Add name-based resolution to gh project item-edit", "base_commit": "9d95dd7344abe6388b59e25b8db54a3c7371cd6a", "merge_commit": "d1897f89b294fc172e5b2993325ad1a0184f711d", "prompt": "Add name-based resolution to gh project item-edit\n\nProposal: #13816\nTracking issue: github/planning-tracking#3431\n\n> **Companion PR:** The `item-list` read path ships as a separate, independently-mergeable PR \u2014 #13823. Both PRs add their own copy of the small `<path>` file, with identical contents. Because the two copies match exactly, Git merges them into one automatically, so the PRs can land in either order without conflicts.\n\n## What this does\n\nLets `gh project item-edit` address ProjectV2 fields, single-select options, and items by **name** instead of node IDs. Existing ID-based flags are fully backward compatible; the name flags are additive and optional.\n\n### `item-edit` (new name-based mode)\nMirrors `item-add` addressing:\n\n```\ngh project item-edit <project-number> --owner <login> --url <issue-or-pr-url> --field \"Status\" --value \"In Progress\"\n```\n\n- `[<project-number>]` positional + `--owner` resolve the project.\n- `--url` resolves the item within that project.\n- `--field <name>` resolves the field; `--value <value>` is dispatched by the field's data type (text/number/date, and for single-select the value is resolved as an option **name**).\n\nhttps://github.com/user-attachments/assets/4e7dceb1-0e4f-4706-9d95-37570a56cdcd\n\n### Mutual exclusivity (enforced in code)\nEvery name/ID pair fails clearly when both are set, never silently merged: `--field` xor `--field-id`, `--url` xor `--id`, and `--value` xor the typed value flags (`--text`/`--number`/`--date`/`--single-select-option-id`/`--iteration-id`). Name-based flags also require the project number argument. Invalid combos are rejected before any GraphQL request fires.\n\n### Errors are self-correctable\nName resolution happens before the ID-typed GraphQL mutation, so a bad name fails loudly with candidate names rather than `400 []`. Six failure modes mirror the sibling MCP PR (github/github-mcp-server#2760): field-not-found, field-ambiguous, option-not-found, option-ambiguous, wrong-field-type, item-not-in-project \u2014 surfaced as human-readable stderr messages listing candidates (CLI convention; not JSON).\n\n### Name matching is case-insensitive\nField and option names are matched with `strings.EqualFold` to mirror the Projects v2 platform API, which normalizes field names via a `name_slug` and downcases single-select option names before matching. Field names are case-insensitively unique on the platform; option names are not, so a case-only option collision still surfaces the `option_ambiguous` error.\n\n## Backwards compatibility\nThe command previously marked `--id` as a required cobra flag; this PR removes that requirement so an item can also be addressed by `--url`, but `--id` remains required in effect via a runtime guard (one of `--id` or `--url` must be set). Existing `--id`-based invocations are unchanged.\n\n## Out of scope / follow-ups\n- **`item-list` named columns** \u2014 ships as a separate, independently-mergeable PR (#13823).\n- MultiSelect and issue-field-value node types: the shared resolver dispatches option resolution by field data type so adding these later is additive, not a rewrite.\n- Bounding/paginating large read payloads (github/planning-tracking#3332).\n\n## Coordination with the sibling MCP PR (github/github-mcp-server#2760)\n- **Case-insensitive matching:** this PR matches names case-insensitively (see above). MCP #2760's resolver currently does case-sensitive matching and should be aligned so both surfaces behave identically.\n- **Read-path mutual exclusivity:** the companion `item-list` PR (#13823) enforces reject-both for its name/ID field selector. MCP #2760's read path currently silently merges `fields`+`field_names`; that should be aligned to the same reject-both behavior.", "test_files": ["pkg/cmd/project/item-edit/item_edit_test.go", "pkg/cmd/project/shared/queries/resolve_fields_test.go", "pkg/cmd/project/shared/queries/resolve_test.go"], "test_cmd": "go test ./pkg/cmd/project/item-edit/... ./pkg/cmd/project/shared/queries/...", "code_files": ["pkg/cmd/project/item-edit/item_edit.go", "pkg/cmd/project/shared/queries/queries.go", "pkg/cmd/project/shared/queries/resolve.go", "pkg/cmd/project/shared/queries/resolve_fields.go"], "additions": 1432, "verified": "fails at parent, passes with the fix", "grader": "cli", "invented_symbols": ["ResolveFieldByName", "number32"]}
{"pr": 13784, "title": "Support antigravity-cli and antigravity2.0 in gh skill", "base_commit": "397876d3dc2828f1c932c651b31ab78e72506da4", "merge_commit": "326faaac8b5c3b736ddc6bfa573cc97dc6452a24", "prompt": "Support antigravity-cli and antigravity2.0 in gh skill\n\n### Description\n\n`gh skill` recognized only one Antigravity surface. This registers `antigravity-cli` and `antigravity2.0` as skill agent hosts across every `gh skill` subcommand, at both project and user scope.\n\nFixes #13767\n\n### Key Points\n\nAntigravity ships several surfaces that share one project skills dir but read user-scope skills from different dirs. The two new [registry entries](https://github.com/cli/cli/blob/8c6bac8c9/internal/skills/registry/registry.go#L91-L104) map each surface to its documented path:\n\n```go\n// Both share the .agents/skills project dir; user dirs differ per surface.\nUserDir: \".gemini/antigravity-cli/skills\" // antigravity-cli\nUserDir: \".gemini/config/skills\"          // antigravity2.0\n```\n\nBecause both reuse `.agents/skills` for the project dir, selecting them next to other `.agents/skills` agents still writes each skill once. Nothing else needs wiring: `--agent`, `--scope`, `InstallDir`, help text, and enum suggestions all derive from the registry slice.\n\n### Notes for reviewers\n\nStart in the [`registry` package](https://github.com/cli/cli/blob/8c6bac8c9/internal/skills/registry/registry.go), where the `AgentHost` entries live. The [registry tests](https://github.com/cli/cli/blob/8c6bac8c9/internal/skills/registry/registry_test.go) extend the existing `TestFindByID` and `TestInstallDir` tables with project- and user-scope cases for both agents.\n\nThe user-scope paths come from Antigravity's own docs, confirmed by hands-on testing (see Additional Context). `.gemini/config/skills` is the one global dir every surface reads, so `antigravity2.0` uses it; the CLI reads its own `.gemini/antigravity-cli/skills`.\n\n### Additional Context\n\n- [Antigravity skills docs](https://antigravity.google/docs/skills) give the official per-surface skill directories.\n- [Where does Antigravity look for Agent Skills?](https://atamel.dev/posts/2026/07-01_where_agy_agent_skills/) verifies by experiment which dir each surface actually reads.\n- [Choosing your surface](https://cloud.google.com/blog/topics/developers-practitioners/choosing-your-surface-antigravity-20-antigravity-cli-antigravity-ide-or-antigravity-sdk) explains the 2.0 / CLI / IDE split.", "test_files": ["internal/skills/registry/registry_test.go"], "test_cmd": "go test ./internal/skills/registry/...", "code_files": ["internal/skills/registry/registry.go"], "additions": 53, "verified": "fails at parent, passes with the fix", "grader": "unit", "invented_symbols": []}
{"pr": 13766, "title": "fix(skills): honor --dir without agent prompt", "base_commit": "ff4e4a255006ca98e3036bd0f8d98374a405835f", "merge_commit": "f1d112104821b055bb0c5656f2989f9213db71f6", "prompt": "fix(skills): honor --dir without agent prompt\n\n## Summary\n- skip the target-agent prompt when `gh skill install` is given `--dir`\n- keep using the default agent host internally only for shared post-install messaging / telemetry\n- add regression coverage for the interactive `--dir` path\n\nFixes #13763.\n\n## Testing\n- not run locally (avoided local Go test/build in this environment)", "test_files": ["pkg/cmd/skills/install/install_test.go"], "test_cmd": "go test ./pkg/cmd/skills/install/...", "code_files": ["pkg/cmd/skills/install/install.go"], "additions": 34, "verified": "fails at parent, passes with the fix", "grader": "unit", "invented_symbols": []}
{"pr": 13723, "title": "Allow downloading release assets without authentication", "base_commit": "954ffc37e5931a9621868a50f05c8fb17e288f19", "merge_commit": "71fb4f5f4358c44c9328637752f0c15a47d84139", "prompt": "Allow downloading release assets without authentication\n\nFixes https://github.com/cli/cli/issues/2680\n\n### Description\n\n`gh release download` now works without authentication against public repositories, matching `gh extension install`. A token is still used when one is present.\n\n### Key Points\n\nThis change has three parts, from simplest to subtlest:\n\n- **Why drop the auth gate** - the login requirement is unnecessary for public downloads.\n- **Fixing the by-tag race to be success oriented** - an anonymous draft-lookup 403 no longer masks a release the REST lookup found.\n- **Honoring the gate under repo override** - the cobra plumbing fix that makes the dropped gate apply to `download`.\n\n#### Why drop the auth gate\n\nRelease endpoints for public repositories are readable anonymously over REST, so the login gate is unnecessary. Removing it is the same one-line `cmdutil.DisableAuthCheck` change that #13176 made for `gh extension install`.\n\nThe same logic here applies as with `gh extension install`:\n\n> What this says is that extension installation is going to happen unauthenticated whether we make it easier or not. We might as well make it easier and keep people installing through our core commands.\n\nIn `gh release download` - people are going to just `curl` the endpoint directly, and that degrades their experience since we provide a lot of abstractions over the API that make `gh` much more than a `curl`/`gh api` call.\n\nBut I'll broaden the conversation - we got feedback that folks are using `gh release download` more in CI and they want to secure their CI by not providing `gh` a token unless it's necessary. If you're downloading a public asset, that's not necessary and we force you to use a larger than necessary token scope.\n\n#### Fixing the by-tag race to be success oriented\n\nThe by-tag path creates problems that make this deviate a bit from `gh ext install` - that's one reason why this diff is a bit bigger. [`FetchRelease`](https://github.com/cli/cli/blob/f0a128ad1c33a41b7d1bdc93b5d507c842713d8a/pkg/cmd/release/shared/fetch.go) races a published-release REST lookup against a draft-release GraphQL lookup. GraphQL rejects anonymous requests, so the draft lookup returns a 403. The old selector returned the first result unless it was exactly `ErrReleaseNotFound`, so that 403 could win the race and mask a release the REST lookup found.\n\nThe selector now prefers a found release and only errors when both lookups fail. We used to return whichever result arrived first, treating only a not-found error as a reason to wait for the second:\n\n```go\n// Before - a non-not-found error from either lookup wins\nres := <-results\nif errors.Is(res.error, ErrReleaseNotFound) {\n    res = <-results\n    cancel()\n} else {\n    cancel()\n    <-results // drain the channel\n}\nreturn res.release, res.error\n```\n\nNow we take a success from either lookup, and only surface an error when both fail:\n\n```go\n// Now - a found release wins; an error needs both lookups to fail\nfirst := <-results\nif first.error == nil {\n    cancel()\n    <-results // drain the channel\n    return first.release, nil\n}\n\nsecond := <-results\ncancel()\nif second.error == nil {\n    return second.release, nil\n}\nif errors.Is(second.error, ErrReleaseNotFound) {\n    return nil, second.error\n}\nreturn nil, first.error\n```\n\nThis also stops a transient draft-lookup failure from masking a real release for authenticated users.\n\n#### Honoring the gate under repo override\n\nRemoving the gate did not take effect on its own. `release` enables the `-R/--repo` override, and that installs a `PersistentPreRunE` on the `release` command. cobra runs only the nearest such hook walking up from the command you invoked, so the override hook shadows the [root auth gate](https://github.com/cli/cli/blob/7b681a4e8b67d203ccdaab5ff54570bdf6ca3669/pkg/cmd/root/root.go). The override re-runs the nearest ancestor hook to make up for that, but it handed *the ancestor* to the hook as the command, so the auth gate judged `release` instead of `download` and never saw download's `DisableAuthCheck`.\n\nThe fix keeps the invoked command and passes that leaf up, the node cobra would have judged if the repo override didn't muck with it:\n\n```go\n// Before - climbs by reassigning cmd, so the gate is judged against the ancestor\nfor cmd.HasParent() {\n    cmd = cmd.Parent()\n    if cmd.PersistentPreRunE != nil {\n        return cmd.PersistentPreRunE(cmd, args)\n    }\n}\n\n// Now - keep the invoked leaf and pass it up\nfor p := overrideCmd.Parent(); p != nil; p = p.Parent() {\n    if p.PersistentPreRunE != nil {\n        return p.PersistentPreRunE(cmd, args)\n    }\n}\n```\n\nThis mirrors [cobra's own `execute` loop](https://github.com/spf13/cobra/blob/v1.10.2/command.go#L984-L986), which walks the parents but always hands them the leaf command. cobra's `EnableTraverseRunHooks` global would run the whole chain for us and maybe is a better long term fix, but it is process-wide and would change pre-run behavior for every command, so I'm not touching it here.\n\nI guarded this with an integration test, since the bug only surfaces with the pieces wired together. [`Test_EnableRepoOverride_authCheckIntegration`](https://github.com/cli/cli/blob/7b681a4e8b67d203ccdaab5ff54570bdf6ca3669/pkg/cmdutil/repo_override_test.go) builds a root gate, a repo-override parent, and a leaf, then asserts the gate judged the leaf: opting out with `DisableAuthCheck` skips the check, otherwise the gate still runs. It lives in `cmdutil` beside the helper, since the coupling is the helper's, not any one command's.\n\n### Notes for reviewers\n\nThree atomic commits:\n\n- `fix(release): don't let a failed draft lookup mask a found release` changes the selector in [`FetchRelease`](https://github.com/cli/cli/blob/f0a128ad1c33a41b7d1bdc93b5d507c842713d8a/pkg/cmd/release/shared/fetch.go) and adds a regression case to the existing `Test_downloadRun` table.\n- `feat(release): allow download without authentication` removes the gate in the [download command](https://github.com/cli/cli/blob/f0a128ad1c33a41b7d1bdc93b5d507c842713d8a/pkg/cmd/release/download/download.go).\n- `fix(cmdutil): honor DisableAuthCheck under repo-override parents` fixes [the repo-override helper](https://github.com/cli/cli/blob/7b681a4e8b67d203ccdaab5ff54570bdf6ca3669/pkg/cmdutil/repo_override.go) so the gate removal actually takes effect, and adds an integration test that pins the coupling between repo override and the root auth gate.\n\n### Additional Context\n- #2680 is the standing request to allow unauthenticated public requests, and its motivating example is `gh release download` on a public repo.\n- #13176 removed the same gate for `gh extension install` and is the precedent this follows.", "test_files": ["pkg/cmd/release/download/download_test.go", "pkg/cmdutil/repo_override_test.go"], "test_cmd": "go test ./pkg/cmd/release/download/... ./pkg/cmdutil/...", "code_files": ["pkg/cmd/release/download/download.go", "pkg/cmd/release/shared/fetch.go", "pkg/cmdutil/repo_override.go"], "additions": 142, "verified": "fails at parent, passes with the fix", "grader": "unit", "invented_symbols": []}
{"pr": 13722, "title": "Detect additional third-party coding agents", "base_commit": "dd26eb39b04db5bd282509787d2b8fbff11ba694", "merge_commit": "561d9f919cbdadc22f886a3eadb260ba0b4f49c5", "prompt": "Detect additional third-party coding agents\n\n### Description\n\nExtends agent detection so the User-Agent header can identify more third-party coding agents driving `gh`. Adds nine new agents and an extra environment-variable signal for one existing one.\n\n### Key Points\n\nNew agents detected: antigravity, augment-cli, replit, goose, cowork, cursor, cursor-cli, kiro, and pi. One existing agent gains an extra signal: claude-code also matches `CLAUDE_CODE`.\n\nDetection order encodes confidence. `AI_AGENT` still wins, followed by agent-specific variables. Two signals are deliberately low-confidence and ranked accordingly:\n\n- `REPL_ID` is present throughout any Replit environment, not only when a Replit agent drives the CLI, so it is annotated as broad in the code.\n- `GOOSE_PROVIDER` only means Goose is configured as a model provider, not that it is running, so Goose is checked absolutely last and any stronger signal wins.\n\nThe single-source block now matches precisely instead of loosely: `kiro` is an exact `TERM_PROGRAM` match, and `pi` is anchored to a path boundary (`/.pi/agent`, or the Windows `\\.pi\\agent`) so an incidental substring cannot trigger it.\n\nWe considered a Devin signal and dropped it. The only reliable marker is the existence of a `/opt/.devin` directory, and this detector is intentionally environment-only with no filesystem access. The environment-variable alternatives (for example an `EDITOR` substring) could produce false positives, and Devin's developer-terminal footprint is small, so a noisy signal was not worth the telemetry pollution.\n\n### Notes for reviewers\n\nTwo files change. Start in the [`agents` detector](https://github.com/cli/cli/blob/1d4ca33b34646ce1165067377fd89f01148cbaf3/internal/agents/detect.go): the constant block lists the new identifiers, and `detectWith` runs the ordered checks top to bottom, returning on first match. Read it straight down to see the precedence. Then the [detector tests](https://github.com/cli/cli/blob/1d4ca33b34646ce1165067377fd89f01148cbaf3/internal/agents/detect_test.go) add table cases for every new agent and signal, plus the precedence and negative cases (exact `kiro`, path-boundary `pi`, Goose losing to stronger signals).\n\nFirst-party documentation for each signal lives inline next to its check, or is marked `// No first-party docs` where none exists.", "test_files": ["internal/agents/detect_test.go"], "test_cmd": "go test ./internal/agents/...", "code_files": ["internal/agents/detect.go"], "additions": 190, "verified": "fails at parent, passes with the fix", "grader": "unit", "invented_symbols": []}
{"pr": 13681, "title": "fix(skills): install universal agent to ~/.agents/skills", "base_commit": "70bb306bd25eb407f90eabefd98824aed62cf519", "merge_commit": "cd68c69dbd5ea0793cece6355f0911ca6a68ffc1", "prompt": "fix(skills): install universal agent to ~/.agents/skills\n\nThe Universal agent host installed user-scoped skills to ~/.config/agents/skills, which is not the location scanned by the majority of compliant clients (Copilot CLI, Pi, OpenCode).\n\nPer the agentskills.io cross-client convention (https://agentskills.io/client-implementation/adding-skills-support), user-level skills for compliant clients live under ~/.agents/skills.\n\nThis PR switches UserDir for the universal agent to the shared project skills directory constant, matching the convention already used by Warp and Cline. Adds regression tests covering both project and user scope.\n\nFixes #13494", "test_files": ["internal/skills/registry/registry_test.go"], "test_cmd": "go test ./internal/skills/registry/...", "code_files": ["internal/skills/registry/registry.go"], "additions": 20, "verified": "fails at parent, passes with the fix", "grader": "unit", "invented_symbols": []}
{"pr": 13679, "title": "fix: show checks summary when all checks were cancelled", "base_commit": "70bb306bd25eb407f90eabefd98824aed62cf519", "merge_commit": "23f83e6ae18998fcd6ac0497f57a1d9ebd95862a", "prompt": "fix: show checks summary when all checks were cancelled\n\n### Description\n\n`gh pr checks` prints a blank summary when a PR's only checks were **cancelled**.\n\n`printSummary` (`<path>`) gates the summary block on:\n\n```go\nif counts.Failed+counts.Passed+counts.Skipping+counts.Pending > 0 {\n```\n\n`counts.Canceled` is omitted, so a cancelled-only result sums to 0, the block is skipped, and the `else if counts.Canceled > 0 { \"Some checks were cancelled\" }` branch becomes unreachable in exactly the case it was written for. The existing \"some cancelled\" test masked this by pairing a cancelled check with passing ones.\n\n### Fix\n\nAdd `counts.Canceled` to the guard.\n\n### Testing\n\nAdded `TestPrintSummary` (`<path>`) with a cancelled-only case asserting the \"Some checks were cancelled\" summary renders. Fails before, passes after; `go test ./pkg/cmd/pr/checks/` green.\n\nReported in #13680.", "test_files": ["pkg/cmd/pr/checks/output_test.go"], "test_cmd": "go test ./pkg/cmd/pr/checks/...", "code_files": ["pkg/cmd/pr/checks/output.go"], "additions": 56, "verified": "fails at parent, passes with the fix", "grader": "unit", "invented_symbols": []}
{"pr": 13675, "title": "Fix flaky TestHuhPrompterMultiSelectWithSearchPersistence on slow architectures", "base_commit": "015e6cf6ac39ad18138f868545f02359c3c8d3ef", "merge_commit": "954ffc37e5931a9621868a50f05c8fb17e288f19", "prompt": "Fix flaky TestHuhPrompterMultiSelectWithSearchPersistence on slow architectures\n\nFixes https://github.com/cli/cli/issues/13727\r\n\r\nThe test was using a fixed 50ms sleep (`waitForOptions()`) to wait for an async bubbletea search goroutine before sending the next keystroke. On slow architectures such as s390x under QEMU emulation, the goroutine scheduling and event loop round-trip can exceed 50ms, causing the `enter` key to arrive while `m.loading` is still `true`. In that state the field silently drops all keystrokes, the form never advances, and the 5s overall deadline fires with \"form.Run() did not complete in time\".\r\n\r\nThe fix replaces the blind sleep with proper channel-based synchronization. An `onSearchDone func()` hook is added to `multiSelectSearchField` (nil in production, set only in tests) and called at the end of `applySearchResult`. A new test helper `waitForSearch(field)` wires a one-shot `sync.Once`-guarded channel into that hook and blocks until it fires - guaranteeing the search is truly complete before the next step is sent, regardless of scheduler timing or machine speed.\r\n\r\n## Error log\r\n```\r\n[  770s] --- FAIL: TestHuhPrompterMultiSelectWithSearchPersistence (5.42s)\r\n[  770s]     --- FAIL: TestHuhPrompterMultiSelectWithSearchPersistence/selections_persist_after_changing_search_query (5.31s)                                                                                                      Context\r\n[  770s]         <path>:478: form.Run() did not complete in time                                                                                                                                                     54,494 tokens\r\n[  770s] FAIL                                                                                                                                                                                                                      5% used\r\n[  770s] FAIL  github.com/cli/cli/v2/internal/prompter  11.841s\r\n```", "test_files": ["internal/prompter/huh_prompter_test.go"], "test_cmd": "go test ./internal/prompter/...", "code_files": ["internal/prompter/multi_select_with_search.go"], "additions": 69, "verified": "fails at parent, passes with the fix", "grader": "cli", "invented_symbols": ["onSearchDone"]}
{"pr": 13624, "title": "Gracefully handle failed GitHub verifier initialization caused by missing trusted root", "base_commit": "bd81afda9ab653bd880298e6cfbf143541522a2f", "merge_commit": "d51b72243a01729cdc76a5b57a0866a2f1bfd2bc", "prompt": "Gracefully handle failed GitHub verifier initialization caused by missing trusted root\n\n\r\n\r\nGracefully handle the failure to initialize the GitHub verifier when the trusted root is missing.", "test_files": ["pkg/cmd/attestation/verification/sigstore_integration_test.go", "pkg/cmd/attestation/verification/sigstore_test.go"], "test_cmd": "go test ./pkg/cmd/attestation/verification/...", "code_files": ["pkg/cmd/attestation/verification/sigstore.go"], "additions": 39, "verified": "fails at parent, passes with the fix", "grader": "unit", "invented_symbols": []}
{"pr": 13620, "title": "feat(discussion): add comment command", "base_commit": "4166ecf2cada209714af79387a5243b334af764b", "merge_commit": "42db02df0c55f8ae556b15ca353887ee4043cd88", "prompt": "feat(discussion): add comment command\n\n## Add `gh discussion comment` command\r\n\r\nThis PR adds a new `gh discussion comment` command for managing comments and replies on GitHub Discussions, along with UX improvements to the `view` command.\r\n\r\n### `gh discussion comment`\r\n\r\nThe command supports adding, editing, and deleting comments and replies. The positional argument determines the action context:\r\n\r\n**Adding a top-level comment:**\r\n```\r\ngh discussion comment 123 --body \"Thanks for raising this\"\r\ngh discussion comment 123 --body-file response.md\r\n```\r\n\r\n**Replying to a comment (positional arg is a comment ID or URL):**\r\n```\r\ngh discussion comment DC_abc123 --body \"Good point\"\r\ngh discussion comment \"https://github.com/OWNER/REPO/discussions/123#discussioncomment-456\" --body \"I agree\"\r\n```\r\n\r\n**Editing a comment or reply:**\r\n```\r\ngh discussion comment DC_abc123 --edit --body \"Updated text\"\r\ngh discussion comment \"https://github.com/OWNER/REPO/discussions/123#discussioncomment-456\" --edit --body-file updated.md\r\n```\r\n\r\n**Deleting a comment or reply:**\r\n```\r\ngh discussion comment DC_abc123 --delete --yes\r\ngh discussion comment \"https://github.com/OWNER/REPO/discussions/123#discussioncomment-456\" --delete --yes\r\n```\r\n\r\nWhen no `--body` or `--body-file` is provided in an interactive terminal, an editor is opened for input. For edits, the editor is pre-populated with the existing comment body.\r\n\r\nComment URLs are resolved to node IDs via msgpack encoding of the repository database ID and comment database ID extracted from the URL fragment.\r\n\r\n### Changes to `gh discussion view`\r\n\r\nThe `--replies` flag now accepts both comment node IDs and comment URLs:\r\n\r\n```\r\ngh discussion view 123 --replies DC_abc123\r\ngh discussion view 123 --replies \"https://github.com/OWNER/REPO/discussions/123#discussioncomment-456\"\r\n```\r\n\r\nWhen a URL is provided, the repository and discussion number from the URL override the positional argument.\r\n\r\n### Tests\r\n\r\n- Added acceptance tests for the `comment` command covering add, reply, edit (including from file), delete (with confirmation prompt check), and URL-based operations.\r\n- Updated existing acceptance tests (`discussion-view.txtar`, `discussion-list.txtar`) to use the new `comment` command instead of raw `gh api graphql` mutations.\r\n- Added a test case in `discussion-view.txtar` to verify `--replies` works with a comment URL.\r\n- Unit tests cover flag parsing, all run paths (add/reply/edit/delete), comment URL resolution, body-file/stdin input, editor pre-population, and error cases.", "test_files": ["pkg/cmd/discussion/client/client_impl_test.go", "pkg/cmd/discussion/comment/comment_test.go", "pkg/cmd/discussion/shared/lookup_test.go", "pkg/cmd/discussion/view/view_test.go"], "test_cmd": "go test ./pkg/cmd/discussion/client/... ./pkg/cmd/discussion/comment/... ./pkg/cmd/discussion/shared/... ./pkg/cmd/discussion/view/...", "code_files": ["pkg/cmd/discussion/client/client.go", "pkg/cmd/discussion/client/client_impl.go", "pkg/cmd/discussion/client/client_mock.go", "pkg/cmd/discussion/client/types.go", "pkg/cmd/discussion/comment/comment.go", "pkg/cmd/discussion/create/create.go", "pkg/cmd/discussion/discussion.go", "pkg/cmd/discussion/edit/edit.go", "pkg/cmd/discussion/shared/lookup.go", "pkg/cmd/discussion/view/view.go"], "additions": 2666, "verified": "fails at parent, passes with the fix", "grader": "cli", "invented_symbols": ["ParseDiscussionOrCommentArg", "CommentOptions", "CommentDatabaseID"]}
{"pr": 13581, "title": "Auto-install official extension stubs in CI", "base_commit": "46480afb125d852c3edd2b4850a97c09b65c64cf", "merge_commit": "7ef9c54bf87fb17516e93a7ddb932efb7c660a35", "prompt": "Auto-install official extension stubs in CI\n\n### Description\n\nSkip the install prompt when `CI` is detected so official extension stubs (like `gh aw`, `gh stack`) auto-install instead of blocking on `Y/n` in CI runners and agent sessions.\n\n### Key Points\n\n- The previous version of this diff mirrored [`runCopilot`](https://github.com/cli/cli/blob/trunk/pkg/cmd/copilot/copilot.go) exactly: TTY always wins, so a pseudo-TTY agent session with `CI=1` still hung on the prompt. This revision flips the order so `CI` wins over TTY.\n- Intentional divergence from `gh copilot`: a follow-up could apply the same change there for consistency, but extension stubs are the more pressing case (agents call `gh <extname>` frequently).\n- `CI` is detected via [`ci.IsCI()`](https://github.com/cli/cli/blob/trunk/internal/ci/ci.go) (any of `CI`, `BUILD_NUMBER`, or `RUN_ID`).\n\n### Notes for reviewers\n\nTwo commits, reviewable in order:\n\n1. [`fix(extension): auto-install official extension stubs in CI`](https://github.com/cli/cli/pull/13581/commits/ef8c7d8ab5c5539a5279f0c05d2d83d6a39f22be) is the behavior change. The [`official_extension_stub`](https://github.com/cli/cli/blob/ef8c7d8ab5c5539a5279f0c05d2d83d6a39f22be/pkg/cmd/root/official_extension_stub.go) file holds the new branching; the [stub test](https://github.com/cli/cli/blob/ef8c7d8ab5c5539a5279f0c05d2d83d6a39f22be/pkg/cmd/root/official_extension_stub_test.go) extends the existing run-function table test and clears CI env vars per subtest; the [acceptance test](https://github.com/cli/cli/blob/ef8c7d8ab5c5539a5279f0c05d2d83d6a39f22be/acceptance/testdata/telemetry/telemetry-for-official-extension-stub.txtar) clears CI env vars and expects non-zero exit plus install instructions on stderr.\n2. [`docs(extension): explain CI bypass in stub install logic`](https://github.com/cli/cli/pull/13581/commits/61a752068e271b699b67bdb39b90a10549adf332) is a one-line comment addressing review feedback that the non-TTY+CI behavior wasn't obvious from the code.\n\n### Additional Context\n\nManually verified locally with `gh stack` uninstalled. The TTY+CI case is the one this revision actually fixes versus the previous version of the diff:\n\n```\n$ CI=1 ./bin/gh stack --help              # TTY + CI\nSuccessfully installed github/gh-stack    # exit 0\n\n$ CI=1 ./bin/gh stack --help < /dev/null  # non-TTY + CI\nSuccessfully installed github/gh-stack    # exit 0\n\n$ ./bin/gh stack --help < /dev/null       # non-TTY, no CI\ngh stack is available as an official extension.\nTo install it, run:\n  gh extension install github/gh-stack    # exit 1\n```", "test_files": ["pkg/cmd/root/official_extension_stub_test.go"], "test_cmd": "go test ./pkg/cmd/root/...", "code_files": ["pkg/cmd/root/official_extension_stub.go"], "additions": 57, "verified": "fails at parent, passes with the fix", "grader": "unit", "invented_symbols": []}
{"pr": 13580, "title": "feat: add `repo read-file` and `repo read-dir`", "base_commit": "d05c71a9da4bce4d57f622aacc6a8ee24e5e158e", "merge_commit": "733e35c04c4141468f8882d17b063414ffe36c57", "prompt": "feat: add `repo read-file` and `repo read-dir`\n\n## Summary\r\n\r\nThis PR adds two new (preview) commands for reading repository contents without cloning.\r\n\r\n### `gh repo read-file`\r\n\r\n```\r\ngh repo read-file <path> [--ref <ref>] [--output <path>] [--clobber] [--unsafe] [--json <fields>] [--jq <expression>] [--template <string>] [--repo <owner/repo>]\r\n```\r\n\r\nReads a single file from a repository and writes it to stdout (or to a file with `--output`).\r\n\r\n### `gh repo read-dir`\r\n\r\n```\r\ngh repo read-dir [<path>] [--ref <ref>] [--json <fields>] [--jq <expression>] [--template <string>] [--repo <owner/repo>]\r\n```\r\n\r\nLists the entries of a directory in a repository, showing each entry's type, name, and size.\r\n\r\nBoth commands are marked **preview**, default to the current repository's default branch (overridable with `--ref`), and support `--json`/`--jq`/`--template` for scripting.\r\n\r\n## Test plan\r\n\r\nYou can verify the command behaviour via acceptance tests and the following examples.\r\n\r\nThe commands below target public repositories so they can be run as-is.\r\n\r\n### `gh repo read-file`\r\n\r\n```sh\r\n# Read a file to stdout\r\ngh repo read-file README.md --repo cli/cli\r\n\r\n# Read from a specific tag/branch/commit\r\ngh repo read-file README.md --ref v2.0.0 --repo cli/cli\r\n\r\n# Write to a file, then confirm --clobber is required to overwrite\r\ngh repo read-file README.md --output <path> --repo cli/cli\r\ngh repo read-file README.md --output <path> --repo cli/cli            # should error: already exists\r\ngh repo read-file README.md --output <path> --clobber --repo cli/cli  # should succeed\r\n\r\n# JSON output, with jq and template\r\ngh repo read-file README.md --json path,size,type --repo cli/cli\r\ngh repo read-file README.md --json path,size --jq '.size' --repo cli/cli\r\ngh repo read-file README.md --template '{{.path}} ({{.size}} bytes){{\"\\n\"}}' --repo cli/cli\r\n\r\n# Error: path does not exist\r\ngh repo read-file <path> --repo cli/cli\r\n```\r\n\r\n### `gh repo read-dir`\r\n\r\n```sh\r\n# List the repository root\r\ngh repo read-dir --repo cli/cli\r\n\r\n# List a subdirectory (note executables render as `file*`)\r\ngh repo read-dir script --repo cli/cli\r\n\r\n# Mixed entry types in one listing: regular files, a symlink (RelNotes),\r\n# and a submodule (sha1collisiondetection)\r\ngh repo read-dir --repo git/git\r\n\r\n# List from a specific tag/branch/commit\r\ngh repo read-dir docs --ref v2.0.0 --repo cli/cli\r\n\r\n# Non-TTY / scriptable output (tab separated)\r\ngh repo read-dir script --repo cli/cli | cat\r\n\r\n# JSON output, with jq and template\r\ngh repo read-dir script --json name,type,mode,modeOctal --repo cli/cli\r\ngh repo read-dir script --json name,type --jq '.entries[] | select(.type==\"file\") | .name' --repo cli/cli\r\ngh repo read-dir --template '{{range .entries}}{{.type}}: {{.name}}{{\"\\n\"}}{{end}}' --repo cli/cli\r\n\r\n# Error: path points to a file, not a directory\r\ngh repo read-dir README.md --repo cli/cli\r\n\r\n# Error: path or ref does not exist\r\ngh repo read-dir nope/nope --repo cli/cli\r\n```", "test_files": ["internal/text/text_test.go", "pkg/cmd/repo/read-dir/read_dir_test.go", "pkg/cmd/repo/read-file/read_file_test.go"], "test_cmd": "go test ./internal/text/... ./pkg/cmd/repo/read-dir/... ./pkg/cmd/repo/read-file/...", "code_files": ["internal/text/text.go", "pkg/cmd/repo/read-dir/http.go", "pkg/cmd/repo/read-dir/read_dir.go", "pkg/cmd/repo/read-file/http.go", "pkg/cmd/repo/read-file/read_file.go", "pkg/cmd/repo/repo.go"], "additions": 2535, "verified": "fails at parent, passes with the fix", "grader": "cli", "invented_symbols": ["FormatSize", "NewCmdReadDir", "NewCmdReadFile"]}
{"pr": 13548, "title": "feat(skills): list available skills when install runs non-interactively", "base_commit": "01bcd474447b5034da20686c838ae4b0cf2b23f5", "merge_commit": "70bb306bd25eb407f90eabefd98824aed62cf519", "prompt": "feat(skills): list available skills when install runs non-interactively\n\n### Summary\n\nRunning `gh skill install <repo>` (or `--from-local <dir>`) without a skill name in a non-interactive context previously errored with `must specify a skill name when not running interactively`. That made discovery awkward when piping or scripting \u2014 users had to switch to an interactive shell just to see what skills a repo offered.\n\nThis PR changes that behavior so the discovered skills are printed instead, mirroring (approximately) what the interactive picker shows. The list is rendered through `tableprinter`, so it auto-switches between a friendly TTY table and tab-separated `SKILL\\tDESCRIPTION` rows when piped \u2014 making it easy to grep / fzf / awk.\n\n### Before\n\n```console\n$ gh skill install github/awesome-copilot | grep review\nmust specify a skill name when not running interactively\n```\n\n### After\n\n```console\n$ gh skill install github/awesome-copilot | grep review\ncode-review     Reviews pull requests\n\u2026\n```\n\nRe-running with a specific skill name installs as before. Same behavior for `--from-local`.\n\n### Context\n\nCame out of a Slack thread where this was identified as a UX gap: interactive mode supports browsing-then-installing, and non-interactive mode should provide the same discoverability so it composes with standard Unix tooling.\n\n### Changes\n\n- `selectSkillsWithSelector` now prints discovered skills via a new `listAvailableSkills` helper instead of erroring when `!canPrompt`.\n- Sentinel `errSkillsListed` lets `installRun` / `runLocalInstall` bail cleanly without going into the install path.\n- Descriptions are pre-fetched (same `fetchDescriptions` callback the picker uses) so the listed output is useful, not bare.\n- Help text + an example for piping into `grep` updated.\n- Tests: replaced the old \"errors\" case with a remote listing assertion and added an equivalent local install case.\n\n### Verification\n\n- `go test ./pkg/cmd/skills/...` \u2705\n- `make lint` \u2705", "test_files": ["pkg/cmd/skills/install/install_test.go"], "test_cmd": "go test ./pkg/cmd/skills/install/...", "code_files": ["pkg/cmd/skills/install/install.go"], "additions": 105, "verified": "fails at parent, passes with the fix", "grader": "unit", "invented_symbols": []}
{"pr": 13541, "title": "feat: add `discussion` command set", "base_commit": "5c9ec1c5f51d682dbf65b0c16f856d8b9303adff", "merge_commit": "52e8e7427702e023d743318f748fceda4c0dcf37", "prompt": "feat: add `discussion` command set\n\nThis PR adds the `gh discussion` command group (list, view, create, edit) as a preview feature.\r\n\r\nHuge thanks to @maxbeizer for doing the heavy lifting on this. :copilot: was also instrumental in implementation, review, and polish throughout.\r\n\r\nI rewrote the commit history (81 commits squashed into 6) for easier review and a cleaner git log. Note that the commits are structured to split the work logically; intermediate commits may not compile independently.\r\n\r\n## Commands\r\n\r\n### `discussion list`\r\n\r\n```shell\r\ngh discussion list [--category <name>] [--state <open|closed|all>] [--author <handle>] [--label <name>] [--search <query>] [--limit <n>] [--json <fields>] [--web]\r\n```\r\n\r\nList discussions in a repository with optional filters. ([8b73951](https://github.com/cli/cli/commit/8b73951ca75c6495a776e36da9fb48cc93000866))\r\n\r\n### `discussion view`\r\n\r\n```\r\ngh discussion view {<number> | <url>} [--comments [--limit <n>]] [--replies <comment-id> [--limit <n>]] [--json <fields>] [--web]\r\n```\r\n\r\nView a discussion's details, comments, or replies to a specific comment. ([57008e7](https://github.com/cli/cli/commit/57008e797097e9b275ba7d1849ac750e762743e1))\r\n\r\n### `discussion create`\r\n\r\n```\r\ngh discussion create --title <text> --body <text> --category <name> [--label <name>,...] [--body-file <path>]\r\n```\r\n\r\nCreate a new discussion, interactively or via flags. ([7bd67a8](https://github.com/cli/cli/commit/7bd67a840456436a1edc0f80dc85687f29b277af))\r\n\r\n### `discussion edit`\r\n\r\n```\r\ngh discussion edit {<number> | <url>} [--title <text>] [--body <text>] [--body-file <path>] [--category <name>] [--add-label <name>,...] [--remove-label <name>,...]\r\n```\r\n\r\nEdit a discussion's title, body, category, or labels, interactively or via flags. ([797effe](https://github.com/cli/cli/commit/797effe0295914e03f4b40cb873276da1d3b7e93))\r\n\r\n## Reusable components\r\n\r\n- **`<path>`**: GraphQL client for Discussions covering list, search, get, create, update, and label mutations with full pagination support. ([51b7653](https://github.com/cli/cli/commit/51b765381870dbf62390e0f7a297cc57ea771db7))\r\n- **`<path>`**: Shared utilities including argument parsing, category matching, label resolution, and client factory helpers. ([d3a1538](https://github.com/cli/cli/commit/d3a153872b9774009d620d0f3e12f993a21765eb))\r\n\r\n## Screenshots\r\n\r\n### `gh discussion list`\r\n\r\n![Terminal output showing a table of discussions with columns for number, state, title, category, author, and date](https://github.com/user-attachments/assets/094587eb-93f8-4b55-8bf9-5ed0965cc67d)\r\n\r\n### `gh discussion view 9094`\r\n\r\n![Terminal output showing discussion details including title, metadata, labels, and body text](https://github.com/user-attachments/assets/55dcf8c0-c4e0-4bc0-b073-8025c5490107)\r\n\r\n### `gh discussion view 9094 --comments --limit 1`\r\n\r\n![Terminal output showing a discussion with its threaded comments displayed below the body](https://github.com/user-attachments/assets/64ea48f7-2cd6-404e-b261-14eaf9a26eb1)\r\n\r\n### `gh discussion view 9094 --replies DC_kwDODKw3uc4AkHLS --limit 1`\r\n\r\n![Terminal output showing paginated replies to a specific discussion comment](https://github.com/user-attachments/assets/87bad4fe-82f3-4124-91c4-c581d9e2a0ef)\r\n\r\n### `gh discussion create` (non-interactive)\r\n\r\n![Terminal showing discussion creation via flags with title, body, category, and label options](https://github.com/user-attachments/assets/bf65e362-7983-4b97-af48-2840fa74c3b9)\r\n\r\n### `gh discussion create` (interactive, category selection)\r\n\r\n![Terminal showing an interactive prompt listing available discussion categories to choose from](https://github.com/user-attachments/assets/7fdcf50f-b74d-4e5d-87a3-9a05aa9bbc78)\r\n\r\n### `gh discussion create` (interactive)\r\n\r\n![Terminal showing interactive prompts for title and body input during discussion creation](https://github.com/user-attachments/assets/764ea1e8-2225-4c6f-9803-77e438e1d1fa)\r\n\r\n### `gh discussion edit` (interactive)\r\n\r\n1. Select fields to update\r\n![Terminal showing a multi-select prompt asking which fields to edit: title, body, category](https://github.com/user-attachments/assets/be05bef6-4dce-44bf-a78e-9b907ef70d46)\r\n\r\n2. Populate new values\r\n![Terminal showing input prompts pre-filled with current values for the selected fields](https://github.com/user-attachments/assets/b2bb8fa8-b0e0-475a-a7ff-a7d6fcf29b0b)\r\n\r\n3. Submit\r\n![Terminal showing the discussion URL printed after a successful edit](https://github.com/user-attachments/assets/9d8e9c31-020f-4961-8ce7-d43bf685ca0d)\r\n\r\n### `gh discussion edit` (non-interactive)\r\n\r\n![Terminal showing discussion edit via flags updating title, body, and labels in one command](https://github.com/user-attachments/assets/5fc60e1b-227a-4ee1-b5b7-2b6281561c5f)", "test_files": ["acceptance/acceptance_test.go", "pkg/cmd/discussion/client/client_test.go", "pkg/cmd/discussion/comment/comment_test.go", "pkg/cmd/discussion/create/create_test.go", "pkg/cmd/discussion/edit/edit_test.go", "pkg/cmd/discussion/list/list_test.go", "pkg/cmd/discussion/shared/labels_test.go", "pkg/cmd/discussion/shared/lookup_test.go", "pkg/cmd/discussion/view/view_test.go"], "test_cmd": "go test ./acceptance/... ./pkg/cmd/discussion/client/... ./pkg/cmd/discussion/comment/... ./pkg/cmd/discussion/create/... ./pkg/cmd/discussion/edit/... ./pkg/cmd/discussion/list/... ./pkg/cmd/discussion/shared/... ./pkg/cmd/discussion/view/...", "code_files": ["pkg/cmd/discussion/client/client.go", "pkg/cmd/discussion/client/client_mock.go", "pkg/cmd/discussion/client/types.go", "pkg/cmd/discussion/comment/comment.go", "pkg/cmd/discussion/create/create.go", "pkg/cmd/discussion/discussion.go", "pkg/cmd/discussion/edit/edit.go", "pkg/cmd/discussion/list/list.go", "pkg/cmd/discussion/shared/categories.go", "pkg/cmd/discussion/shared/client.go", "pkg/cmd/discussion/shared/labels.go", "pkg/cmd/discussion/shared/lookup.go", "pkg/cmd/discussion/view/view.go", "pkg/cmd/root/root.go"], "additions": 13075, "verified": "fails at parent, passes with the fix", "grader": "cli", "invented_symbols": ["DiscussionClient"]}
{"pr": 13523, "title": "Support custom CLAUDE_CONFIG_DIR in install", "base_commit": "9a593ce81b593dee752cc11737d1a3ef768e52b3", "merge_commit": "412c5fae63239705c0120e76ec9508307d2709a0", "prompt": "Support custom CLAUDE_CONFIG_DIR in install\n\n\r\n\r\nFixes https://github.com/cli/cli/issues/13262\r\n\r\n## Summary\r\n\r\nThis updates Claude Code user-scope skill directory resolution to honor `CLAUDE_CONFIG_DIR` when it is set. User-scope installs for Claude Code now write to `$CLAUDE_CONFIG_DIR/skills`, falling back to the existing `~/.claude/skills` location when the environment variable is not present.\r\n\r\nThe change lives in the shared agent registry path resolver so `gh skill install`, `gh skill list`, and `gh skill update` all use the same location for Claude Code user-scope skills.\r\n\r\n## Tests\r\n\r\n- Added registry coverage for Claude Code's default user-scope path and `CLAUDE_CONFIG_DIR` override.\r\n- Added an install regression test that verifies `gh skill install --agent claude-code --scope user` writes under `$CLAUDE_CONFIG_DIR/skills` and not `~/.claude/skills`.\r\n- Ran `go test ./internal/skills/registry ./pkg/cmd/skills/...`.", "test_files": ["internal/skills/registry/registry_test.go", "pkg/cmd/skills/install/install_test.go"], "test_cmd": "go test ./internal/skills/registry/... ./pkg/cmd/skills/install/...", "code_files": ["internal/skills/registry/registry.go"], "additions": 69, "verified": "fails at parent, passes with the fix", "grader": "cli", "invented_symbols": ["claudeConfigDirEnv", "All"]}
{"pr": 13471, "title": "add --all flag to install all skills in a repo", "base_commit": "8bd56966ac5e2fe7d3162ad424dd11d877f4815e", "merge_commit": "e6fa2faf9e3203c2caaa54dcb1526c5f093fa998", "prompt": "add --all flag to install all skills in a repo\n\nFixes https://github.com/cli/cli/issues/13453\r\n\r\n## Summary\r\n- Add `--all` to `gh skill install` so users can install every discovered skill from a repository without providing an individual skill name.\r\n- Reuse the existing all-skills selection path, including collision detection, so skills that would overwrite each other still fail with guidance.\r\n- Update help text, examples, and tests for flag parsing, validation, and multi-skill remote installs.\r\n\r\n## Testing\r\n- `go test ./pkg/cmd/skills/install`", "test_files": ["pkg/cmd/skills/install/install_test.go"], "test_cmd": "go test ./pkg/cmd/skills/install/...", "code_files": ["pkg/cmd/skills/install/install.go"], "additions": 78, "verified": "fails at parent, passes with the fix", "grader": "cli", "invented_symbols": ["All"]}
{"pr": 13469, "title": "Improve update --all command if some skills don't have metadata", "base_commit": "81ed6d4e355446dbb4c1a7b038534948f7c195b8", "merge_commit": "5cfb648ace667b394728332d492b3e1a017baafa", "prompt": "Improve update --all command if some skills don't have metadata\n\n\r\n\r\nFixes https://github.com/cli/cli/issues/13369\r\n\r\n## Summary\r\n\r\nFixes `gh skill update --all` so it no longer prompts for skills that are missing GitHub metadata.\r\n\r\nWhen `--all` is used, skills without GitHub metadata are skipped with a notice, matching the flag's \"without prompting\" behavior.\r\n\r\n## Changes\r\n\r\n- Skip missing-metadata skills when `--all` is used instead of prompting interactively.\r\n- Keep the warning summary so users know which skills were skipped.\r\n- Update the warning text to explain that users can run `gh skill update <skill>` interactively to add metadata, or reinstall the skill.\r\n- Add a regression test to verify `--all` does not prompt for missing metadata.\r\n\r\n## Testing\r\n\r\n- `go test ./pkg/cmd/skills/update/... -run TestUpdateRun -count=1`\r\n- `go test ./pkg/cmd/skills/... -count=1`", "test_files": ["pkg/cmd/skills/update/update_test.go"], "test_cmd": "go test ./pkg/cmd/skills/update/...", "code_files": ["pkg/cmd/skills/update/update.go"], "additions": 39, "verified": "fails at parent, passes with the fix", "grader": "unit", "invented_symbols": []}
{"pr": 13459, "title": "fix discovery support in nested dirs", "base_commit": "8bd56966ac5e2fe7d3162ad424dd11d877f4815e", "merge_commit": "7f885723a9cb72cd4318eef3c831f8a7bb9d9093", "prompt": "fix discovery support in nested dirs\n\n\r\n\r\nFixes https://github.com/cli/cli/issues/13372\r\n\r\n## Summary\r\n\r\nFix `gh skill install` and `gh skill preview` handling for nested skill paths.\r\n\r\n## What changed\r\n\r\n- Allow `--allow-hidden-dirs` to discover hidden skill directories at any repository depth, not only at the root.\r\n- Treat deeply nested skill arguments as exact repo-relative paths in both `install` and `preview`.\r\n  - Example: `packages/agent-skills/my-skill`\r\n- Preserve one-slash selectors like `owner/skill` as namespaced skill names, not paths.\r\n- Share path-detection logic between `install` and `preview`.\r\n- Add regression tests for nested hidden directories, arbitrary nested path installs, and arbitrary nested path previews.\r\n\r\n## Product note\r\n\r\nThis does not change the recommended Agent Skills publishing conventions or broaden search/publish behavior. Non-standard layouts are only supported when the user explicitly provides an exact path.\r\n\r\n## Testing\r\n\r\n- `go test ./internal/skills/discovery ./pkg/cmd/skills/install ./pkg/cmd/skills/preview -run 'TestIsSkillPath|Test_isSkillPath|TestPreviewRun|TestMatchHiddenDirConventions|TestDiscoverSkillsWithOptions|TestDiscoverLocalSkillsWithOptions|TestInstallRun' -count=1`\r\n- `go test ./internal/skills/discovery ./pkg/cmd/skills/... -count=1`\r\n- `git diff --check`", "test_files": ["internal/skills/discovery/discovery_test.go", "pkg/cmd/skills/install/install_test.go", "pkg/cmd/skills/preview/preview_test.go"], "test_cmd": "go test ./internal/skills/discovery/... ./pkg/cmd/skills/install/... ./pkg/cmd/skills/preview/...", "code_files": ["internal/skills/discovery/discovery.go", "pkg/cmd/skills/install/install.go", "pkg/cmd/skills/preview/preview.go"], "additions": 271, "verified": "fails at parent, passes with the fix", "grader": "cli", "invented_symbols": ["DiscoverSkillByPathWithOptions", "All"]}
{"pr": 13449, "title": "fix(skills): stage updates in a temp dir and swap in-place", "base_commit": "a6012aca41ffa2d943ea9f12094a7398ce2e4b7e", "merge_commit": "01bcd474447b5034da20686c838ae4b0cf2b23f5", "prompt": "fix(skills): stage updates in a temp dir and swap in-place\n\nCloses #13370.\n\n## Problem\n\n`gh skill update` could relocate skills and delete the original install directory:\n\n- With `--dir`, namespaced layouts (`{dir}/{namespace}/{name}/SKILL.md`) had their install base computed by walking `filepath.Dir` twice, putting the new install one level too shallow. The post-install migration step then `RemoveAll`ed the original namespaced directory, taking the user's files with it.\n- Without `--dir`, the same migration cleanup quietly relocated namespaced skills found under an agent install directory to a flat layout, even though users expect updates to land where the skill currently lives.\n\nEither path breaks symlinks, mounts, and any external references that point at the skill directory.\n\n## Fix\n\nEach update is staged into a sibling directory of the existing skill dir (same filesystem, so renames are atomic). On success the contents are swapped in via per-entry rename:\n\n1. Move every existing entry from the skill dir into a sibling backup dir.\n2. Move every staged entry into the skill dir.\n3. Remove the backup.\n\nIf any step fails, the rollback path removes any freshly installed entries and moves the originals back from the backup.\n\nThis guarantees:\n\n- All updates happen in-place, regardless of layout (flat vs. namespaced) and regardless of whether the skill was found via an agent host or via `--dir`.\n- The skill directory's inode is preserved, so symlinks, mounts, and external references continue to resolve.\n- Stale files from the previous version are removed.\n- A failure at any point (install, read, rename) leaves the existing skill completely untouched.\n\n## Tests\n\n- New acceptance test `<path>` plants a namespaced skill, runs `gh skill update --dir`, and asserts the skill remains at the original namespaced path with no flat-path duplicate.\n- New unit test `TestSwapDirectoryContents_PreservesDestInode` asserts the skill directory's identity is preserved across a successful swap (via `os.SameFile`).\n- New unit test `TestSwapDirectoryContents_RollsBackOnFailure` exercises the rollback path and asserts the original content (including nested subdirs) is fully restored.\n- The \"namespaced skill with `--dir`\" unit test is updated to assert in-place behavior and stale-file cleanup.\n- The existing `install_failure_during_update_reports_error_and_continues` unit test had a harness shortcut that silently skipped its `verify` block whenever `wantErr` was set. That shortcut is removed so the \"preserve original on install failure\" assertion is now actually enforced.", "test_files": ["pkg/cmd/skills/update/update_test.go"], "test_cmd": "go test ./pkg/cmd/skills/update/...", "code_files": ["pkg/cmd/skills/update/update.go"], "additions": 242, "verified": "fails at parent, passes with the fix", "grader": "cli", "invented_symbols": ["swapDirectoryContents"]}
{"pr": 13430, "title": "Derive digest algorithm from ref length in release verify commands", "base_commit": "b99d73d7421ae182b8ab3f5b4469dff93c792da9", "merge_commit": "f1c10e032a636540a5e28705682e203b118a47fa", "prompt": "Derive digest algorithm from ref length in release verify commands\n\nFixes #13429\n\n### Summary\n\nThe `gh release verify` and `gh release verify-asset` commands hard-coded a `sha1:` prefix when constructing the digest identifier for a release tag's commit SHA. Once GitHub repositories using SHA-256 commit digests are supported, that ref will be a 64-character SHA-256 hash and labeling it as `sha1:` is both misleading in user-facing output and incorrect for the attestation lookup.\n\n### Changes\n\n- Added a small shared helper `DigestAlgForRef(digest string) string` in `<path>` that returns `\"sha256\"` for 64-character digests and `\"sha1\"` otherwise (preserving existing behavior).\n- Replaced the hard-coded `\"sha1\"` argument in `<path>` and `<path>` with a call to the new helper.\n- Added unit tests for `DigestAlgForRef` covering SHA-1, SHA-256, empty, and unexpected lengths.\n- Added `*_SHA256` variants of the existing `FailedNoAttestations` tests in both commands that assert the error message includes `sha256:<digest>`.\n\n### Verification\n\n- `go test ./pkg/cmd/release/shared/... ./pkg/cmd/release/verify/... ./pkg/cmd/release/verify-asset/...` passes.\n- `go build ./...` passes.\n- Existing SHA-1 tests continue to pass (no regression).", "test_files": ["pkg/cmd/release/shared/fetch_test.go", "pkg/cmd/release/verify-asset/verify_asset_test.go", "pkg/cmd/release/verify/verify_test.go"], "test_cmd": "go test ./pkg/cmd/release/shared/... ./pkg/cmd/release/verify/... ./pkg/cmd/release/verify-asset/...", "code_files": ["pkg/cmd/release/shared/fetch.go", "pkg/cmd/release/verify-asset/verify_asset.go", "pkg/cmd/release/verify/verify.go"], "additions": 153, "verified": "fails at parent, passes with the fix", "grader": "cli", "invented_symbols": ["DigestAlgForRef"]}
{"pr": 13421, "title": "Allow agents as application for secrets", "base_commit": "909cbd7d8834b5fb63d671a55a540c2cfa04d669", "merge_commit": "e60f00e30880079adefd258c388c642b1e97ddf6", "prompt": "Allow agents as application for secrets\n\nFixes https://github.com/cli/cli/issues/13419\r\n\r\nI built it locally and ran `bin/gh secret set TEST_SECRET -a agents -b 123 -o <my-org>` and it worked exactly as expected.\r\n\r\nWhat might be missing is another test? I looked through it and added a little bit but maybe a maintainer has a little more input regarding that.", "test_files": ["pkg/cmd/secret/delete/delete_test.go", "pkg/cmd/secret/list/list_test.go", "pkg/cmd/secret/set/set_test.go", "pkg/cmd/secret/shared/shared_test.go"], "test_cmd": "go test ./pkg/cmd/secret/delete/... ./pkg/cmd/secret/list/... ./pkg/cmd/secret/set/... ./pkg/cmd/secret/shared/...", "code_files": ["pkg/cmd/secret/delete/delete.go", "pkg/cmd/secret/list/list.go", "pkg/cmd/secret/secret.go", "pkg/cmd/secret/set/set.go", "pkg/cmd/secret/shared/shared.go"], "additions": 199, "verified": "fails at parent, passes with the fix", "grader": "cli", "invented_symbols": ["Agents", "shared.Agents"]}
{"pr": 13418, "title": "New `gh skill list` to inventory installed agent skills", "base_commit": "e6fa2faf9e3203c2caaa54dcb1526c5f093fa998", "merge_commit": "674e02a56b020549b45f970808ce7c20e179824a", "prompt": "New `gh skill list` to inventory installed agent skills\n\nCloses #13245 \r\n\r\n## Summary\r\n\r\nAdds `gh skill list` to inventory installed agent skills across known agent host directories.\r\n\r\n- Scans project and user skill install paths for all registered agent hosts\r\n- Deduplicates shared install directories like `.agents/skills`\r\n- Supports filtering with `--agent`, `--scope`, and custom scanning with `--dir`\r\n- Renders a concise table with `NAME`, `AGENT`, `SCOPE`, and `SOURCE`\r\n- Supports parseable output via `--json`, `--jq`, and `--template` with fields for `skillName`, `hosts`, `scope`, `sourceURL`, `version`, `pinned`, and `path`\r\n- Records privacy-safe `skill_list` telemetry with filters, output format, and result count\r\n\r\n\r\n## Testing\r\n\r\n- `go test ./pkg/cmd/skills/list/...`\r\n- `go test ./pkg/cmd/skills/...`", "test_files": ["pkg/cmd/skills/list/list_test.go"], "test_cmd": "go test ./pkg/cmd/skills/list/...", "code_files": ["pkg/cmd/skills/list/list.go", "pkg/cmd/skills/skills.go"], "additions": 1123, "verified": "fails at parent, passes with the fix", "grader": "cli", "invented_symbols": ["ListOptions"]}
{"pr": 13413, "title": "Overwrite existing parent on `gh issue edit --add-sub-issue`", "base_commit": "6106bd30764b17caac56fa57876ff9f5536093d4", "merge_commit": "b08503368202901bbc1b6fc7936dff650eb32012", "prompt": "Overwrite existing parent on `gh issue edit --add-sub-issue`\n\nFix the `Sub issue may only have one parent` 422 that surfaces when `--add-sub-issue` is handed a candidate that already belongs to another parent. Pass `replaceParent: true` so the take-over is silent.\r\n\r\nStacked on top of #13412 (which is on top of #13411).", "test_files": ["acceptance/acceptance_test.go", "pkg/cmd/issue/edit/edit_test.go"], "test_cmd": "go test ./acceptance/... ./pkg/cmd/issue/edit/...", "code_files": ["api/queries_issue.go"], "additions": 221, "verified": "fails at parent, passes with the fix", "grader": "unit", "invented_symbols": []}
{"pr": 13412, "title": "Rename Editable.Allowed to Selectable", "base_commit": "72859237e26439cfc23be9fb2a8144dc441a0826", "merge_commit": "2503dcfd79e372bfade8f41d05a2fbae57da1e26", "prompt": "Rename Editable.Allowed to Selectable\n\nAddresses [thread r3196020651](https://github.com/cli/cli/pull/13057#discussion_r3196020651) on #13057.\r\n\r\nStacked on top of #13411 (`<path>-feedback`) since the field name is leveraged in the parent Issues 2.0 PR.", "test_files": ["acceptance/acceptance_test.go", "pkg/cmd/issue/edit/edit_test.go", "pkg/cmd/pr/shared/survey_test.go"], "test_cmd": "go test ./acceptance/... ./pkg/cmd/issue/edit/... ./pkg/cmd/pr/shared/...", "code_files": ["api/queries_issue.go", "pkg/cmd/issue/edit/edit.go", "pkg/cmd/pr/edit/edit.go", "pkg/cmd/pr/shared/editable.go"], "additions": 241, "verified": "fails at parent, passes with the fix", "grader": "cli", "invented_symbols": ["Selectable"]}
{"pr": 13403, "title": "Use int64 for GitHub database IDs", "base_commit": "1e6bb88c6d8503189bca408f7be162018b1acf84", "merge_commit": "0274077b56a5ef8e575358721149cd02888b2a5f", "prompt": "Use int64 for GitHub database IDs\n\n## Use `int64` for GitHub database IDs\r\n\r\nCloses #9247\r\n\r\n### Problem\r\n\r\nSeveral structs in the CLI use Go's `int` type for GitHub database ID fields. Go's `int` is platform-dependent and could be 32-bit on some architectures, while GitHub internally uses 64-bit integers for database IDs. The [REST API OpenAPI spec](https://github.com/github/rest-api-description) explicitly declares many resource IDs as `integer` with `format: int64` (e.g. repositories, GPG keys, SSH keys), and the [GraphQL API has migrated some `databaseId` fields from `Int` to `BigInt`](https://docs.github.com/en/graphql/overview/breaking-changes).\r\n\r\n### Changes\r\n\r\nUpdated all struct fields representing GitHub database IDs from `int` to `int64`, along with their downstream usage:\r\n\r\n- **Codespaces**: `Repository.ID`, `CreateCodespaceParams.RepositoryID`, `startCreateRequest.RepositoryID`, plus interface/mock/test cascading\r\n- **Rulesets**: `RulesetGraphQL.DatabaseId`, `RulesetREST.Id`, `BypassActors.ActorId`, `RulesetRule.RulesetId`, and the skills `rulesetsResponse.ID`\r\n- **Keys**: `deployKey.ID`, `gpgKey.ID`, `sshKey.ID`\r\n- **Cache**: `Cache.Id`, plus `parseCacheID` and `deleteCacheByID` (which still used `int`/`strconv.Atoi`)\r\n- **Autolinks**: `Autolink.ID`\r\n- **Agent tasks**: `JobActor.ID`, `JobPullRequest.ID`\r\n\r\nAll `strconv.Itoa()` calls on these fields were updated to `strconv.FormatInt(x, 10)`.\r\n\r\nGraphQL response structs were also audited - only `RulesetGraphQL.DatabaseId` needed updating. Others were already correct (`GitHubUser.DatabaseID` is `int64`, `FullDatabaseID` is `string` for GraphQL `BigInt`).\r\n\r\n### Future: Custom linter to prevent regressions\r\n\r\nA custom `go/analysis` linter that flags struct fields with ID-like names or JSON tags still using `int` has been prototyped on a separate branch: [`9247-idtype-linter`](https://github.com/cli/cli/compare/trunk...9247-idtype-linter). It's split out to keep this PR focused on the type changes. The linter correctly identifies all the violations fixed here.\r\n\r\n<details>\r\n<summary>Linter output BEFORE this PR (27 violations)</summary>\r\n\r\n```\r\n<path>:38:2: struct field ID looks like a GitHub database ID but uses int; use int64 instead\r\n<path>:43:2: struct field ID looks like a GitHub database ID but uses int; use int64 instead\r\n<path>:20:2: struct field ID looks like a GitHub database ID but uses int; use int64 instead\r\n<path>:25:2: struct field Id looks like a GitHub database ID but uses int; use int64 instead\r\n<path>:153:2: struct field ID looks like a GitHub database ID but uses int; use int64 instead\r\n<path>:807:2: struct field RepositoryID looks like a GitHub database ID but uses int; use int64 instead\r\n<path>:858:2: struct field RepositoryID looks like a GitHub database ID but uses int; use int64 instead\r\n<path>:203:4: struct field RepoID looks like a GitHub database ID but uses int; use int64 instead\r\n<path>:216:4: struct field RepoID looks like a GitHub database ID but uses int; use int64 instead\r\n<path>:260:4: struct field RepoID looks like a GitHub database ID but uses int; use int64 instead\r\n<path>:591:3: struct field RepoID looks like a GitHub database ID but uses int; use int64 instead\r\n<path>:614:2: struct field RepoID looks like a GitHub database ID but uses int; use int64 instead\r\n<path>:621:3: struct field RepoID looks like a GitHub database ID but uses int; use int64 instead\r\n<path>:639:3: struct field RepoID looks like a GitHub database ID but uses int; use int64 instead\r\n<path>:660:2: struct field RepoID looks like a GitHub database ID but uses int; use int64 instead\r\n<path>:666:3: struct field RepoID looks like a GitHub database ID but uses int; use int64 instead\r\n<path>:858:3: struct field RepoID looks like a GitHub database ID but uses int; use int64 instead\r\n<path>:879:2: struct field RepoID looks like a GitHub database ID but uses int; use int64 instead\r\n<path>:885:3: struct field RepoID looks like a GitHub database ID but uses int; use int64 instead\r\n<path>:14:2: struct field ID looks like a GitHub database ID but uses int; use int64 instead\r\n<path>:6:2: struct field ID looks like a GitHub database ID but uses int; use int64 instead\r\n<path>:16:2: struct field ID looks like a GitHub database ID but uses int; use int64 instead\r\n<path>:13:2: struct field DatabaseId looks like a GitHub database ID but uses int; use int64 instead\r\n<path>:27:2: struct field Id looks like a GitHub database ID but uses int; use int64 instead\r\n<path>:33:3: struct field ActorId looks like a GitHub database ID but uses int; use int64 instead\r\n<path>:53:2: struct field RulesetId looks like a GitHub database ID but uses int; use int64 instead\r\n<path>:67:2: struct field ID looks like a GitHub database ID but uses int; use int64 instead\r\n```\r\n\r\n</details>\r\n\r\n<details>\r\n<summary>Linter output AFTER this PR (0 violations)</summary>\r\n\r\n```\r\n(clean - no output)\r\n```\r\n\r\n</details>\r\n\r\n### Reviewer Notes\r\n\r\nWe should run A/C tests on this before merging.", "test_files": ["pkg/cmd/codespace/create_test.go"], "test_cmd": "go test ./pkg/cmd/codespace/...", "code_files": ["internal/codespaces/api/api.go", "pkg/cmd/agent-task/capi/job.go", "pkg/cmd/cache/delete/delete.go", "pkg/cmd/cache/shared/shared.go", "pkg/cmd/codespace/common.go", "pkg/cmd/codespace/create.go", "pkg/cmd/codespace/mock_api.go", "pkg/cmd/gpg-key/delete/delete.go", "pkg/cmd/gpg-key/delete/http.go", "pkg/cmd/repo/autolink/shared/autolink.go", "pkg/cmd/repo/deploy-key/list/http.go", "pkg/cmd/repo/deploy-key/list/list.go", "pkg/cmd/ruleset/list/list.go", "pkg/cmd/ruleset/shared/shared.go", "pkg/cmd/ruleset/view/view.go", "pkg/cmd/skills/publish/publish.go", "pkg/cmd/ssh-key/list/list.go", "pkg/cmd/ssh-key/shared/user_keys.go"], "additions": 61, "verified": "fails at parent, passes with the fix", "grader": "unit", "invented_symbols": []}
{"pr": 13393, "title": "fix(copilot): hint to run copilot directly when exec fails", "base_commit": "9b505c3fb8c3e52100575adbad2da7c4714684d8", "merge_commit": "fe996d33ac852eafdb2769fcba063860be1b4e6a", "prompt": "fix(copilot): hint to run copilot directly when exec fails\n\n## Summary\n\nWhen `gh copilot` finds the `copilot` binary but fails to execute it (e.g., due to unusual characters like parentheses in the file path on Windows), the error message now includes a hint:\n\n```\nFailed to run '<path>', try running `copilot` directly without `gh`.\n```\n\n## Details\n\nOn Windows, `cmd.exe` shims (e.g., those planted by VS Code or npm) can fail when the path contains special characters like `(`. Since `gh copilot` invokes the binary via `os/exec`, these failures surface as opaque errors. This change adds a practical workaround hint so users can still use Copilot CLI directly.\n\nRelated to #13106", "test_files": ["pkg/cmd/copilot/copilot_test.go"], "test_cmd": "go test ./pkg/cmd/copilot/...", "code_files": ["pkg/cmd/copilot/copilot.go"], "additions": 45, "verified": "fails at parent, passes with the fix", "grader": "cli", "invented_symbols": ["findCopilotBinaryFunc"]}
{"pr": 13362, "title": "Bump `gh copilot` telemetry sampling to 100%", "base_commit": "3c162a78efc4cf48f6582f261223166af2558a64", "merge_commit": "6176c3eee4b70d0ccf3c6a1503035d049ca4e81b", "prompt": "Bump `gh copilot` telemetry sampling to 100%\n\n## Description\r\n\r\nFixes https://github.com/github/cli/issues/1163\r\n\r\nBumps the telemetry sampling rate for the `gh copilot` command from 1% to 100%, to allow for better analysis of usage.", "test_files": ["pkg/cmd/copilot/copilot_test.go"], "test_cmd": "go test ./pkg/cmd/copilot/...", "code_files": ["pkg/cmd/copilot/copilot.go", "pkg/cmd/root/root.go"], "additions": 24, "verified": "fails at parent, passes with the fix", "grader": "unit", "invented_symbols": []}
{"pr": 13068, "title": "Print `gh auth refresh` for 401 returns", "base_commit": "49d4747a81425418dd2e7bee7e33541b7c644ca9", "merge_commit": "7c439196c1fc4d645d091e83029370cef845587c", "prompt": "Print `gh auth refresh` for 401 returns\n\n`gh auth refresh` exists to make it simpler for users to refresh their tokens on expiration/scope mismatch, but help messages only suggest using it in limited scenarios, and not in a common case of a token expiring and the user receiving a 401 error. Now, the auth flow will detect this case, and for refreshable tokens (namely, tokens created by logging in with `gh auth login` in the first place), it will suggest using `gh auth refresh` for these cases.\r\n\r\nFixes https://github.com/cli/cli/issues/13066.", "test_files": ["internal/ghcmd/cmd_test.go", "pkg/cmd/auth/status/status_test.go"], "test_cmd": "go test ./internal/ghcmd/... ./pkg/cmd/auth/status/...", "code_files": ["internal/ghcmd/cmd.go", "pkg/cmd/auth/shared/writeable.go", "pkg/cmd/auth/status/status.go"], "additions": 108, "verified": "fails at parent, passes with the fix", "grader": "cli", "invented_symbols": ["authRecoveryCommand"]}
{"pr": 13057, "title": "Add Issues 2.0 support: issue types, sub-issues, and relationships", "base_commit": "3c162a78efc4cf48f6582f261223166af2558a64", "merge_commit": "20147bf81fed534faf1b5c9be12638983b8e5e82", "prompt": "Add Issues 2.0 support: issue types, sub-issues, and relationships\n\n# Add Issues 2.0 support: issue types, sub-issues, and relationships\n\nCloses #10298\nCloses #9696\nCloses #11757\nCloses #12477\nCloses #12152\n\nAdds support for GitHub's Issues 2.0 features across `gh issue create`, `edit`, `view`, and `list`.\n\n## New flags\n\n### `gh issue create`\n\n| Flag | Description |\n|------|-------------|\n| `--type Bug` | Set the issue type |\n| `--parent 100` | Add as a sub-issue of the specified parent (number or URL) |\n| `--blocked-by 200,201` | Mark as blocked by these issues |\n| `--blocking 300` | Mark as blocking these issues |\n\nInteractive mode prompts for issue type when the repo has types configured.\n\n### `gh issue edit`\n\n| Flag | Description |\n|------|-------------|\n| `--type Bug` | Set the issue type |\n| `--set-parent 100` | Set or replace the parent issue |\n| `--remove-parent` | Remove the parent issue |\n| `--add-sub-issue 123,124` | Add sub-issues |\n| `--remove-sub-issue 123` | Remove a sub-issue |\n| `--add-blocked-by 200` | Add blocked-by relationship |\n| `--remove-blocked-by 201` | Remove blocked-by relationship |\n| `--add-blocking 300,301` | Add blocking relationships |\n| `--remove-blocking 300` | Remove blocking relationship |\n\nInteractive mode adds Type and Parent to the field picker. Batch editing\nsupported for `--type` across multiple issues.\n\n### `gh issue view`\n\nTTY output now displays:\n- Issue type on the state line (e.g., `Bug \u00b7 Open`)\n- Type, Parent, Blocked by, Blocking metadata rows\n- Sub-issues section with completion progress (e.g., `Sub-issues \u00b7 3/5 (60%)`)\n\nJSON output supports new fields: `issueType`, `parent`, `subIssues`,\n`subIssuesSummary`, `blockedBy`, `blocking`. The `subIssues`, `blockedBy`,\nand `blocking` fields are objects of the shape `{ \"nodes\": [...], \"totalCount\": N }`\n(rather than flat arrays) so consumers can detect when the truncation\nlimit is reached. Each node and the `parent` object include the canonical\n`id`.\n\n### `gh issue list`\n\n| Flag | Description |\n|------|-------------|\n| `--type Bug` | Filter issues by type |\n\n## GHES compatibility\n\n- **Issue types and sub-issues**: GA on all supported GHES versions (3.17+). No feature detection needed.\n- **Relationships** (blocked-by/blocking): Requires GHES 3.19+. Feature detection via `__type` schema introspection for the `blockedBy` field on the Issue type. Unsupported GHES versions receive a clear error message. Gated with `// TODO IssueRelationshipsCleanup` for cleanup when GHES 3.18 support ends.\n\n## Implementation details\n\n- Post-creation mutations for type, parent, and relationships (same pattern as projectV2 and assignee mutations)\n- `--blocking` and `--add-blocking` swap API arguments: calls `addBlockedBy(issueId=OTHER, blockingIssueId=THIS)`\n- Cross-repo references supported via URL for `--parent`, `--blocked-by`, `--blocking`, and sub-issue flags\n- Shared helpers extracted to `<path>` (`ResolveIssueRef`, `ResolveIssueTypeName`)\n- Issue-only fields (Type, Parent) gated behind `Allowed` in the Editable pattern to prevent leaking into `gh pr edit`\n- Issue type names with spaces are quoted in search queries (`type:\"Bug Report\"`)\n\n## Testing\n\n- **34 new flag parsing tests** across create, edit, and list\n- **18 new HTTP mock behavior tests** covering mutations, error cases, and GHES unsupported paths\n- **5 new view tests** (TTY rendering, JSON export)\n- **2 regression tests** (issue-only fields don't leak into PR edit, no-new-fields view unchanged)\n\n## Changes\n\n17 files changed, 2105 insertions(+), 18 deletions(-)\n\n| File | Change |\n|------|--------|\n| `<path>` | New types, Issue struct fields, mutation functions, helpers |\n| `<path>` | GraphQL fragment cases, `issueOnlyFields` |\n| `<path>` | ExportData cases for new fields |\n| `<path>` | `IssueRelationshipsSupported` detection |\n| `<path>` | New flags, post-creation mutations, interactive type picker |\n| `<path>` | New flags, mutations, interactive Type/Parent picker |\n| `<path>` | TTY rendering, conditional relationship fields |\n| `<path>` | `--type` filter via search path |\n| `<path>` | Shared `ResolveIssueRef`, `ResolveIssueTypeName` |\n| `<path>` | `IssueType`/`Parent` fields, `Allowed` gating |\n| `<path>` | `IssueType` in `FilterOptions` and search query |\n\n## Key notes for reviewers\n\n- **`--blocking` arg swap**: The GitHub API only has `addBlockedBy(issueId, blockingIssueId)`. To support `--blocking`, we swap the arguments \u2014 the OTHER issue becomes `issueId` and THIS issue becomes `blockingIssueId`. Same for `--remove-blocking`.\n- **Editable `Allowed` gating**: `IssueType` and `Parent` are added to the shared `Editable` struct but gated behind `Allowed = true`, which only the issue edit command sets. This prevents these issue-only fields from leaking into `gh pr edit`'s interactive picker. Regression test in `<path>`.\n- **View relationship fields are GHES-gated**: `blockedBy` and `blocking` are only added to the GraphQL query when `IssueRelationshipsSupported` is true. Without this, `gh issue view` would break on GHES 3.17\u20133.18 where these fields don't exist.\n- **Batch edit type resolution**: When editing multiple issues with `--type`, the type name is resolved to an ID once before the loop rather than per-issue, avoiding N-1 redundant `RepoIssueTypes` API calls.\n\n## Manual test results\n\nAll scenarios tested against on github.com.\n\n<b>24/24 scenarios passing</b> (click to expand)\n\n### Issue types\n\n| # | Scenario | Command | Result |\n|---|----------|---------|--------|\n| 1 | `create-with-type` | `gh issue create --title \"...\" --type Bug` | \u2705 [#1](https://github.com/bagtoad-enterprise-cloud-testing/garbage/issues/1) created, type=Bug verified |\n| 2 | `create-error-invalid-type` | `gh issue create --title \"Oops\" --type Bugz` | \u2705 Error: `type \"Bugz\" not found; available types: Task, Bug, Feature` |\n| 3 | `edit-change-type` | `gh issue edit 1 --type Feature` | \u2705 Type changed to Feature verified |\n| 4 | `create-interactive-type` | `gh issue create` (interactive) | \u2705 [#21](https://github.com/bagtoad-enterprise-cloud-testing/garbage/issues/21) Type picker shown, Bug selected and set |\n\n### Sub-issues\n\n| # | Scenario | Command | Result |\n|---|----------|---------|--------|\n| 5 | `create-with-parent` | `gh issue create --title \"...\" --parent 3` | \u2705 [#4](https://github.com/bagtoad-enterprise-cloud-testing/garbage/issues/4) created as sub-issue of #3 |\n| 6 | `edit-set-parent` | `gh issue edit 5 --set-parent 3` | \u2705 Parent set, verified via `--json parent` |\n| 7 | `edit-remove-parent` | `gh issue edit 5 --remove-parent` | \u2705 Parent removed, verified null |\n| 8 | `edit-add-sub-issues` | `gh issue edit 3 --add-sub-issue 6,7` | \u2705 Sub-issues added, verified via `--json subIssues` |\n| 9 | `edit-remove-sub-issue` | `gh issue edit 3 --remove-sub-issue 6` | \u2705 Sub-issue removed |\n| 10 | `edit-error-mutual-parent` | `gh issue edit 5 --set-parent 1 --remove-parent` | \u2705 Error: `specify only one of --set-parent or --remove-parent` |\n\n### Relationships\n\n| # | Scenario | Command | Result |\n|---|----------|---------|--------|\n| 11 | `create-with-relationships` | `gh issue create --blocked-by 8 --blocking 9` | \u2705 [#11](https://github.com/bagtoad-enterprise-cloud-testing/garbage/issues/11) blockedBy=#8, blocking=#9 verified |\n| 12 | `edit-add-remove-blocked-by` | `gh issue edit 14 --add-blocked-by 12 --remove-blocked-by 13` | \u2705 Relationships updated |\n| 13 | `edit-add-blocking` | `gh issue edit 14 --add-blocking 15,16` | \u2705 Blocking relationships added (swapped args verified) |\n\n### View\n\n| # | Scenario | Verification | Result |\n|---|----------|-------------|--------|\n| 14 | `view-with-type` | TTY shows `Bug \u00b7 Open` on state line, `Type: Bug` row | \u2705 |\n| 15 | `view-parent-sub-issues` | TTY shows Parent row, Sub-issues section with progress | \u2705 |\n| 16 | `view-child-parent` | TTY shows parent reference for child issue | \u2705 |\n| 17 | `view-relationships` | TTY shows Blocked by and Blocking rows | \u2705 |\n| 18 | `view-json-issueType` | `--json issueType` returns id, name, description, color | \u2705 |\n| 19 | `view-json-parent-subIssues` | `--json parent,subIssues,subIssuesSummary` returns structured data | \u2705 |\n| 20 | `view-json-relationships` | `--json blockedBy,blocking` returns `{ nodes: [...], totalCount: N }` with id, number, title, url, state | \u2705 |\n\n### List\n\n| # | Scenario | Command | Result |\n|---|----------|---------|--------|\n| 21 | `list-filter-by-type` | `gh issue list --type Bug` | \u2705 Filtered results returned |\n\n### Combined\n\n| # | Scenario | Command | Result |\n|---|----------|---------|--------|\n| 22 | `create-all-flags` | `gh issue create --type Task --parent 3 --blocked-by 8 --blocking 9` | \u2705 [#17](https://github.com/bagtoad-enterprise-cloud-testing/garbage/issues/17) all fields set |\n| 23 | `edit-batch-type` | `gh issue edit 18 19 20 --type Bug` | \u2705 All 3 issues updated to Bug |\n| 24 | `edit-interactive` | `gh issue edit 21` (interactive) | \u2705 Picker shows Type and Parent fields |\n\n**TODO**: GHES-specific scenarios (`create-error-ghes-relationships`), but GHES feature detection does have unit test coverage with mock introspection responses.", "test_files": ["acceptance/acceptance_test.go", "api/export_pr_test.go", "internal/featuredetection/feature_detection_test.go", "pkg/cmd/issue/create/create_test.go", "pkg/cmd/issue/edit/edit_test.go", "pkg/cmd/issue/list/list_test.go", "pkg/cmd/issue/view/view_test.go", "pkg/cmd/pr/shared/params_test.go", "pkg/cmd/pr/shared/survey_test.go", "pkg/search/query_test.go"], "test_cmd": "go test ./acceptance/... ./api/... ./internal/featuredetection/... ./pkg/cmd/issue/create/... ./pkg/cmd/issue/edit/... ./pkg/cmd/issue/list/... ./pkg/cmd/issue/view/... ./pkg/cmd/pr/shared/... ./pkg/search/...", "code_files": ["api/export_pr.go", "api/queries_issue.go", "api/query_builder.go", "internal/featuredetection/feature_detection.go", "pkg/cmd/issue/create/create.go", "pkg/cmd/issue/edit/edit.go", "pkg/cmd/issue/list/list.go", "pkg/cmd/issue/shared/lookup.go", "pkg/cmd/issue/view/view.go", "pkg/cmd/pr/edit/edit.go", "pkg/cmd/pr/shared/editable.go", "pkg/cmd/pr/shared/params.go", "pkg/search/query.go"], "additions": 2925, "verified": "fails at parent, passes with the fix", "grader": "cli", "invented_symbols": ["IssueRelationshipsSupported", "IssueType"]}
